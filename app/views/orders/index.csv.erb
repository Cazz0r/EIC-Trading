Account,Assigned,Date,Location,Platform,Status,Type,Quantity,Commodity
<% @orders.each do |order| %>
<%= order.account.name %>,<%= order.user ? order.user.username : 'Unassigned' %>,<%= order.created_at.utc.to_s(:db) %> UTC,<%= order.location_text %>,<%= order.platform_text %>,<%= order.status_text %>,<%= order.type_text.downcase %>,<%= order.order_quantity %>,<%= order.order_commodity %>
<% end %>
